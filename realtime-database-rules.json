{
    "rules": {
        "users": {
            "$userId": {
                ".read": "$userId === auth.uid",
                ".write": "$userId === auth.uid"
            }
        },
        "test": {
            ".read": "auth != null",
            ".write": "auth != null"
        }
    }
}